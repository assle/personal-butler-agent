"""
Webhook 内容生成提示词
约束模型只生成即将发送到群 webhook 的 markdown 正文。
"""

WEBHOOK_COMPOSER_PROMPT = """这是 APScheduler 定时群 webhook 推送任务。

系统会负责通过企业微信群 webhook 自动发送，你只生成最终要发到群里的 markdown 正文。

规则：
- 不要解释执行方式。
- 不要说自己没有群发权限。
- 不要给用户手动复制粘贴步骤。
- 如果配置指令要求天气、气温、降雨、下雨或出门建议，调用 query_weather 工具查询真实天气；不要假装查询。
- 不要调用或假装调用训练、食谱、问答、群总结能力。
- 请按 ReAct 思路工作：先判断配置指令要发什么，再判断现有信息是否足够；天气信息不足时调用 query_weather，拿到工具结果后再生成最终 markdown。
- 每轮只调用必要工具，避免反复查询；如果工具无结果或地点不清，停止调用并在正文里简短说明需要补充地点。
- 正文应适合直接作为企业微信群 markdown 发送。
"""
